{
  "name": "bigbang-gallery",
  "version": "2.0.0",
  "description": "Matrix-themed AI art gallery with cosmic visuals",
  "author": "Thomas J Butler",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "deploy:manual": "npm run build && git add dist -f && git commit -m 'Deploy to gh-pages' && git subtree push --prefix dist origin gh-pages"
  },
  "keywords": [
    "ai-art",
    "matrix",
    "gallery",
    "cosmic",
    "midjourney",
    "interactive"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/ThomasJButler/bigbang-gallery.git"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.0.4"
  }
}
